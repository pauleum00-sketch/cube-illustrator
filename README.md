# 큐브 일러스트 제작소

큐브 교본·강의 자료에 넣을 그림을 브라우저에서 바로 만들고 PNG로 내보내는 도구입니다.
설치 없이 웹에서 쓸 수 있고, 파일 하나(`cube-illustrator.html`)로 이루어진 순수 HTML/JS라 내려받아 오프라인으로 써도 됩니다.

**👉 바로 쓰기: https://pauleum00-sketch.github.io/cube-illustrator/**

## 기능

### 큐브 색칠 (입체 뷰)
- 스티커를 클릭해 색칠. 같은 색을 다시 누르면 회색으로 되돌아감
- 9색 팔레트 (큐브 6색 + 마젠타 + 회색 + 자유 색), 배경색 변경 가능
- **층 드래그**: 스티커를 잡고 끌면 그 층이 커서를 따라 돌고, 놓으면 90°에 걸림
- **시뮬레이터**: 회전기호 적용·역적용(케이스 자동 생성)·애니메이션 재생.
  화면에 보이는 방향 기준(앞=F, 위=U)으로 공식 해석 가능
- **단계 프리셋**: 완성 / 크로스 / 1층 / 2층까지를 버튼 하나로 색칠
- 큐브 바깥 빈 곳을 드래그하면 자유 회전, 놓으면 반듯한 각도로 자동 정렬(끌 수 있음)
- X·Y·Z 각도를 슬라이더/숫자로 정밀 조절, 마음에 드는 각도는 슬롯에 저장(최대 5개)
- 드래그 잠그기: 맞춰둔 각도가 실수로 흐트러지지 않게 보호
- 보조면: 화면에서 가려진 면을 펼쳐서 함께 표시. 떨어지는 거리 조절 가능
- 투명 모드: 앞면이 반투명해져 뒷면 스티커가 비쳐 보임 (윤곽선 없는 깔끔한 도해)
- 화면 표시는 고정 프레임이라 회전 중에도 큐브 크기·위치가 튀지 않음

### 탑다운 뷰 (OLL·PLL 도해)
- 윗면 9칸 + 옆면 윗줄 12칸을 위에서 내려다본 표준 도해 형식
- **PLL 화살표**: 스티커 두 개를 차례로 눌러 교환 화살표를 그림 (양방향 지원)
- 입체 뷰와는 독립된 큐브 — 서로 색이 섞이지 않음. 전부 회색에서 시작
- 회전기호 적용·단계 프리셋은 탑다운에서도 동작

### 회전기호 도해
- `R U2 R' U' R U R'` 같은 회전기호를 입력하면 수별 도해를 자동 생성
- 바깥층·두 층(r, u, …)·가운데층(M, E, S)·큐브 전체(x, y, z) 지원
- `U' U'` → `U'2` 자동 정리, 뒤쪽·안쪽이라 보이지 않는 `B`·`S`는 점선 화살표로 표현
- 크기 조절(55%~145%) 후 내보내기

### 공통
- PNG 저장·클립보드 복사 (3배 해상도, 배경 투명 옵션)
- 작업 내용은 브라우저에 자동 저장 — 새로고침해도 그대로 유지
- 입체·탑다운·회전기호가 같은 투시각을 써서 교본 안에서 그림 각도가 일관됨

## 로컬에서 쓰기

저장소를 내려받아 `cube-illustrator.html`을 브라우저로 열면 끝입니다. 서버·빌드 불필요.

## 업데이트 내역

[CHANGELOG.md](CHANGELOG.md)에서 확인할 수 있습니다.

## 만든 사람

큐브 과외를 하며 3x3 초급해법 교본을 직접 쓰다가, 교본에 넣을 그림 도구가 필요해 만들었습니다.
피드백·제안은 이슈로 남겨주세요.
