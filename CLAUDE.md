# 큐브 일러스트 제작소 작업 규칙

큐브 교본용 그림(입체 색칠·탑다운 도해·회전기호 도해)을 만들어 PNG로 내보내는 단일 HTML 도구.
빌드·서버·의존성 없음 — `cube-illustrator.html` 하나가 전부다. 파일을 나누지 말 것.
버전을 올리면 `CHANGELOG.md` 맨 위에도 함께 적는다.

## 위키 연동 규칙

이 프로젝트의 기록은 코드 폴더가 아니라 위키에 남긴다.

위키 문서 경로: `~/내 드라이브/wiki/02 프로젝트/큐브 일러스트 제작.md`
(절대경로: `/Users/pauleum/내 드라이브/wiki/02 프로젝트/큐브 일러스트 제작.md`)

의미 있는 작업(기능 추가, 버그 수정, 구조 변경, 배포)을 마칠 때마다 위 문서의 `## 진행 기록` 섹션 **맨 위에** 아래 형식으로 한 줄에서 세 줄 추가한다. 기존 기록은 지우지 않는다.

```
- YYYY-MM-DD 무엇을 왜 했는지 / 지금 상태 / 다음에 할 것
```

문서의 `현재 상태` 항목도 실제와 다르면 함께 고친다.
작업 종료 시 사용자에게 **'위키 갱신함'** 또는 **'갱신 못 함(이유)'** 을 반드시 한 줄로 보고한다.
코드 자체는 이 폴더 밖으로 복사하지 않는다.
